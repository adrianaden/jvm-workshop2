package org.adenaden.tutorial.jvm.workshop.service;

import org.adenaden.tutorial.jvm.workshop.dto.DokuHostedRequestDTO;
import org.adenaden.tutorial.jvm.workshop.entity.Bill;

public class DokuService {
	
	DokuHostedRequestDTO createTransaction(Bill bill){
		
		
		return DokuHostedRequestDTO.builder().build();
//			.EMAIL(p.getEmail())
//			.MALLID(merchantId)
//			.CHAINMERCHANT("NA")
//			.AMOUNT(decFormat.format(pendaf.getMateri().getBiaya()))
//			.PURCHASEAMOUNT(decFormat.format(pendaf.getMateri().getBiaya()))
//			.TRANSIDMERCHANT(tagihan.getNomorInvoice()).PAYMENTCHANNEL("")
//			.REQUESTDATETIME(dateFormat.format(new Date()))
//			.CURRENCY("360")
//			.PURCHASECURRENCY("360")
//			.SESSIONID("1234567890")
//			.BASKET(keterangan).build();
	}
}
